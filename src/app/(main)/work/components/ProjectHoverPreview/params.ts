import type { SpringOptions } from 'framer-motion';

export const PREVIEW_CELL_WIDTH = 300;
export const PREVIEW_GAP = 0;

export const PREVIEW_SPRING: SpringOptions = {
  stiffness: 380,
  damping: 40,
  mass: 0.7,
  bounce: 0,
};
